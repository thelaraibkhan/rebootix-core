---
summary: "CLI reference for `rebootix docs` (search the live docs index)"
read_when:
  - You want to search the live Rebootix docs from the terminal
---

# `rebootix docs`

Search the live docs index.

```bash
rebootix docs browser extension
rebootix docs sandbox allowHostControl
```
