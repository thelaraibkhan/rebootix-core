---
summary: "CLI reference for `rebootix sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `rebootix sessions`

List stored conversation sessions.

```bash
rebootix sessions
rebootix sessions --active 120
rebootix sessions --json
```
